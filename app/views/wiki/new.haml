%h3= t(:new_wiki_page)
= bootstrap_form_tag :url => new_wiki_page_path , :html => { :multipart => true , :role=>"form"}  do |f|
  .form_group
    = f.text_field :title
  .form_group
    = f.text_area :content 
  .form_group
    = f.submit t(:submit) , :class => "btn btn-info submit"
  